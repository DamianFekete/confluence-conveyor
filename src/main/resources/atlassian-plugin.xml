<atlassian-plugin key="${atlassian.plugin.key}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}"/>
    </plugin-info>

    <!-- Internationalization -->
    <resource name="i18n" type="i18n" location="${package}.i18n"/>

    <component key="conveyorAssistant" name="Conveyor Assistant" class="org.randombits.confluence.conveyor.ConveyorAssistant">
        <description>Keeps track of the conveyor modules and performs the actual overriding.</description>
    </component>

    <module-type key="conveyor" name="Conveyor Module Type" class="org.randombits.confluence.conveyor.DefaultConveyorModuleDescriptor">
		<description>Allows other plugins to define Conveyor XWork override configurations.</description>
	</module-type>

</atlassian-plugin>